import { constants } from 'pp-framework-react';

export const templateList = {
    navLink: {
        color: constants.color[4],
        cursor: 'pointer',
    }
};

export const inputNumber = {
    span: {
        fontSize: '1.3rem',
        margin: 'auto 5px'
    }
};

export const sidebar = {

};

export default {
    templateList
};
